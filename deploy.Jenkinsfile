pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                sh '''
                    # deploying to k8s cluster ..( or any other alternative)
                '''
            }
        }
    }
}